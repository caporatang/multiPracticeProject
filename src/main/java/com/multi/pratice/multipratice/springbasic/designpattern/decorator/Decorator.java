package com.multi.pratice.multipratice.springbasic.designpattern.decorator;

/**
 * packageName : com.multi.pratice.multipratice.springbasic.designpattern.decorator
 * fileName : Decorator
 * author : taeil
 * date : 2023/02/19
 * description :
 * =======================================================
 * DATE          AUTHOR                      NOTE
 * -------------------------------------------------------
 * 2023/02/19        taeil                   최초생성
 */
public class Decorator {
    // 데코레이터 패턴은 기존 뼈대(클래스)는 유지하되, 이후 필요한 형태로 꾸밀 떄 사용
    // 만든 클래스를 예를 들면 ICar(기본 뼈대)를 유지하고,
    // AudiDecorator와 A3-5클래스들을 활용해서 각각 다른 값이 출력 되는것을 확인할수있음

    // 확장이 필요한 경우 상속의 대안으로도 활용함
    // SOLID 중 개방폐쇄 원칙(OCP)과 의존 역전 원칙(DIP)를 따름

}